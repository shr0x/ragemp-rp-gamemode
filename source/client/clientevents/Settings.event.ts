mp.events.add("client::settingsMenu:changeDisplay", (actionData: string) => {
    const {id, action} = JSON.parse(actionData)

})
mp.events.add("client::settingsMenu:changeButton", (actionData: string) => {
    const {id, action} = JSON.parse(actionData)

})